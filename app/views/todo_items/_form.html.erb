<%= simple_form_for([@todo_list, @todo_list.todo_items.build]) do |f| %> 
  <%= f.input :content, placeholder: "New Todo", class: "form-control" %>
  <%= f.button :submit, class: "btn btn-info" %>
<% end %>